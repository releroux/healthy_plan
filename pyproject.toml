[project]
name = "healthy-plan"
version = "0.1.0"
description = "Static site with a high protein low carb meal plan and exercise routine"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "reflex>=0.9,<0.10",
    "pyyaml>=6.0,<7.0",
]

[tool.uv]
package = false
